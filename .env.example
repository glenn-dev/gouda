# Copy this file to .env and fill both values with local-only secrets.
# Empty values fail closed. There are no committed or generated fallbacks.
# .env is ignored by git and must never be committed.
DJANGO_SECRET_KEY=
POSTGRES_PASSWORD=
